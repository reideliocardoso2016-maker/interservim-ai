# Alembic configuration
# This file is created when running: alembic init migrations

# For manual setup, run:
# alembic init -t async migrations
# Then edit alembic.ini and env.py

"""
To initialize Alembic migrations:

1. Install alembic: pip install alembic
2. Run: alembic init migrations
3. Edit alembic.ini: sqlalchemy.url = postgresql://...
4. Edit migrations/env.py to import Base metadata
5. Run: alembic revision --autogenerate -m "initial"
6. Run: alembic upgrade head
"""
